import ImplementPackageTable from '@smd/components/implement-package-table';
import React from 'react';

const ImplementPackagePage: React.FC = () => {
  return (
    <>
      <ImplementPackageTable />
    </>
  );
};

export default ImplementPackagePage;
