import auth from '@app/slices/auth';
import global from '@app/slices/global';
import admin from '@admin/reducers';
import csyt from '@csyt/reducers';
import smd from '@smd/redux';

export default {
  auth,
  global,
  admin,
  csyt,
  smd,
};
