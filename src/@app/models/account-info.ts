export interface AccountInfo {
  aud: string;
  Credential: Credential;
  Email: string;
  exp: number;
  FullName: string;
  Id: string;
  iss: string;
  Role: string | string[];
  Username: string;
}
